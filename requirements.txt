# --- CORE BACKEND ---
Django==5.1.6
gunicorn==23.0.0
django-cors-headers==4.7.0
asgiref==3.8.1
sqlparse==0.5.3

# --- UI & DASHBOARD ---
django-jazzmin==3.0.1

# --- API & REST FRAMEWORK ---
djangorestframework==3.15.2

# --- DATABASE (NEON & POSTGRES) ---
dj-database-url==2.3.0
psycopg2-binary==2.9.10
psycopg==3.2.4
python-dotenv==1.0.1

# --- AI & IMAGE PROCESSING (OPTIMIZED FOR RENDER) ---
# Using tensorflow-cpu to stay within Render's free/starter tier RAM limits
tensorflow-cpu==2.15.0
numpy==1.26.4
pillow==10.4.0

# --- STATIC FILES ---
whitenoise==6.9.0

# --- BACKGROUND TASKS (CELERY + REDIS) ---
celery==5.4.0
django-celery-beat==2.7.0
django-celery-results==2.5.1  # NEW — stores Celery task results in Neon DB (CELERY_RESULT_BACKEND = 'django-db')
django-timezone-field==7.1
redis==5.2.1

# --- SOCIAL AUTH & GOOGLE LOGIN ---
django-allauth==65.3.0         # NEW — Google / social login framework
google-auth==2.38.0            # NEW — verifies Google ID tokens from Flutter
google-auth-httplib2==0.2.0    # NEW — HTTP transport for google-auth
requests-oauthlib==2.0.0       # NEW — OAuth dependency for allauth

# --- PASSWORD SECURITY ---
# No extra package needed — custom validators live in api/validators.py
# Django's built-in AUTH_PASSWORD_VALIDATORS are now enabled in settings.py

# --- SOLAR SCHEDULED EVENTS ---
ephem==4.1.6                   # NEW — enables solar('sunrise', lat, lon) in CELERY_BEAT_SCHEDULE
                               # Used for Maseru sunrise weather sync trigger
                               # Can be removed if you keep the solar block commented out

# --- UTILITIES ---
requests==2.32.3
urllib3==2.3.0
certifi==2025.1.31
